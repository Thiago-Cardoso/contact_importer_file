
  <nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <form class="d-flex">
        <% unless !user_signed_in? %>
          <a class="navbar-brand" href="#">Contact Importer</a>
          <div class="d-grid gap-2 d-md-flex justify-content-md-end">
            <%= link_to "#{current_user.email}", edit_user_registration_path, class: 'btn btn-outline-light', type: "submit" %>
            <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'btn btn-outline-light', type: "submit" %>
          </div>
        <% end %>
      </form>
    </div>
  </div>
</nav>